//import {  } from '../actions/orders';

const initialState = {
    expandOrder: true,
};

const ordersReducer = (state = initialState, action) => {
    
        return state;
    
}

export default ordersReducer;